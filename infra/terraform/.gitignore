# Terraform state and working directories — never commit these
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Real variable values may contain secrets
terraform.tfvars
*.auto.tfvars
